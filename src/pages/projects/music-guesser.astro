---
export const prerender = true;
import ProjectLayout from '../../layouts/ProjectLayout.astro';
import { projects } from '../../data/projects';
import MusicGuesser from '../../components/projects/music-guesser/MusicGuesser';

const project = projects.find((p) => p.slug === 'music-guesser')!;
---

<ProjectLayout project={project}>
  <MusicGuesser client:load />
</ProjectLayout>
